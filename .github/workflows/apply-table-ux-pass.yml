name: Apply table UX pass

on:
  push:
    branches: [master]
    paths:
      - '.github/workflows/apply-table-ux-pass.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.24.0
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Apply table UX changes
        run: node scripts/fix-table-ux-pass.mjs
      - name: Typecheck client
        run: pnpm client:typecheck
      - name: Build client
        run: pnpm client:build
      - name: Commit directly to master
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add client/src/table-3d.ts client/src/table-3d-faces.ts client/src/dev-tuning.ts
          git commit -m "Polish tile interactions and draw animations"
          git push
